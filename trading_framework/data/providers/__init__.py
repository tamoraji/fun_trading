"""Market data provider implementations.

Each provider implements the MarketDataProvider interface from core.
Available providers:
- yahoo   — Yahoo Finance (free, stocks/crypto/forex)
- alpaca  — Alpaca Markets API (free tier, stocks/crypto)  [planned]
- ccxt    — Crypto exchanges via CCXT library              [planned]
- csv     — Load from local CSV files                      [planned]
"""
